{
  "name": "env-tracking",
  "version": "1.0.7",
  "description": "load, create or update environment variables from .env file",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "release:patch": "npm run build && git add . && git commit && npm version patch && npm publish",
    "release:minor": "npm run build && git add . && git commit && npm version minor && npm publish",
    "release:major": "npm run build && git add . && git commit && npm version major && npm publish",
    "test": "node ./dist/test/index.js"
  },
  "keywords": [
    "environment",
    "env",
    ".env",
    "file"
  ],
  "author": "trackingx",
  "license": "ISC",
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
