{
  "name": "env-tracking",
  "version": "1.0.4",
  "description": "load, create or update environment variables from .env file",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "ts-node -e \"import { Environment, env } from './dist'; console.log(Environment.get('APP_NAME'))\""
  },
  "keywords": [
    "env",
    "environment",
    ".env",
    "file"
  ],
  "author": "trackingx",
  "license": "ISC",
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}